interface CardProps {
  title?: any
  subtitle?: any
  elevation?: any
}

export default function Card({ title, subtitle, elevation }: CardProps) {
  return (
    <div className="">
      {/* Card component content */}
    </div>
  )
}